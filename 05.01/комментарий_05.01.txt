  ====================  легенда  ====================

Верно      — всё хорошо и комментариев не требует
Замечания  — в целом верно, но можно лучше / комментарии
Ошибки     — что-то или всё пошло не так
Неверно    — условие задачи неверно интерпретировано
Нет ответа — файл с решением задачи отсутствует



  =====================  задачи  ====================

1.1. Замечания: 
        а) очень неудобно для восприятия записаны условия, что мешает записать -30 < a and a <= 2 ? а ещё лучше -30 < a <= 2

1.2. Верно

1.3. Замечания: 
        а) запись условий
        б) в случаях длинных строк кода стоит переносить часть на новую строку

1.4. Замечания: 
        а) используйте встроенную функцию abs() 

2.1. Замечания: 
        а) когда нам не нужно значение переменной цикла, то вместо неё мы пишем скрытую переменную _
        б) sep='/n'
           во-первых, символ конца строки записывается '\n'
           во-вторых, вы передаёте в функцию print() один аргумент – следовательно значение параметра sep не используется

2.2. Замечания: 
        а) переменная i у вас выступает и как переменная цикла, и изменяется в теле цикла – вы записываете в неё же ввод пользователя. в этом нет никакой необходимости и такого поведения лучше избегать

2.3. Замечания: 
        а) лишние скобки при передаче аргумента в функцию range()

2.4. Замечания: 
        а) что делает третья строка кода?

2.5. Ошибки:
        а) не соблюден формат вывода
        б) разные переменные в вводе и проверке условия
     Замечания: 
        а) вы извернулись, добавив внутри цикла ещё одну проверку второй переменной с принудительным выходом по оператору break
        вместо этого нужно было работать с одной переменной i, если проверяете условие в заголовке цикла; или осуществлять ввод проверку и выход только внутри цикла, а в заголовке оставить True
        б) вместо списка можно было использовать строку:
            
            s = ''
            while True:
                n = int(input())
                s += str(n) + ' '
                if not n % 7:
                    break

2.6. Замечания: 
        а) и снова – что делает третья строка кода?

2.7. Верно
        а) итоговую сумму и надо было вывести – всё правильно

2.8. Нет ответа



Продолжаю настаивать на написании кода в соответствии с PEP8!
https://pep8.org/#other-recommendations

Изучить таблицу приоритета операторов:
https://docs.python.org/3/reference/expressions.html#operator-precedence



  =====================  баллы  =====================

1.1. 4/5
1.2. 2/2
1.3. 4/5
1.4. 3/3
2.1. 2/2
2.2. 2/2
2.3. 3/3
2.4. 3/3
2.5. 1/3
2.6. 2/2
2.7. 3/3
2.8. 0/3